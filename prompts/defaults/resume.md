This résumé is being tailored to the job below. Use the analyzed job data to decide
what to emphasize, the skill inventory as the ONLY skills you may attribute to the
candidate, and the work/projects below as the proof of what they have actually done.

# Job (analyzed)
{job.extracted_description}

# Candidate skill inventory (skills the candidate lists)
Only some of these are backed by real work — see the proof below. Never claim a skill
that is not in this list.

{user.skills}

# Proof — what the candidate has actually built and done (titles and facts are fixed)
Work history:
{user_profile.work_history_indexed}

Projects:
{user_profile.projects_indexed}

# Honesty rules (apply to every section)
- Titles are fixed. NEVER assign a seniority the work history does not show — no
  "senior", "lead", "principal", "staff", or "junior", and never invent a job title
  (e.g. "Machine Learning Engineer") the candidate has not actually held.
- Use proof words — "proven", "track record", "expert", "extensive", "deep" — ONLY for
  skills demonstrated by the work or projects above. For a skill that appears only in
  the inventory with no backing work or project, use lighter framing ("familiar with",
  "working knowledge of", "exposure to") or leave it out.
- Only attach a skill to a role or project that actually used it.
